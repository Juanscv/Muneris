<div class="w-box">
<p><strong>Energy consumption vs. time</strong></p>
<%=	if !@ebills.empty? then
		high_stock('echart', @echart)
	else
		render partial: 'layouts/partials/blank', locals: { section: 'bill' } 
	end %>
</div>
<div class="w-box">
<p><strong>Water consumption vs. time</strong></p>
<%=	if !@wbills.empty? then
		high_stock('wchart', @wchart)
	else
		render partial: 'layouts/partials/blank', locals: { section: 'bill' } 
	end %>
</div>
<div class="w-box">
<p><strong>Gas consumption vs. time</strong></p>
<%=	if !@gbills.empty? then
		high_stock('gchart', @gchart)
	else
		render partial: 'layouts/partials/blank', locals: { section: 'bill' } 
	end %>
</div>



