<div>
  <div class="w-container">
    <div class="w-row">
      <%= render 'layouts/partials/sidebar' %>

      <% if @is_current_user or @current_user.friend_with? @user or @current_user.admin? %>
      <%=  render 'layouts/partials/activity' %>
      <%end%>
      
      <% if @is_current_user or  @current_user.admin? %>
      <% end %>   
    </div>

  </div>
</div>